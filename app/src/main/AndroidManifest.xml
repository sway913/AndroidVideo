<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.net168.androidvideo">

    <uses-permission android:name="android.permission.CAMERA"/>
    
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".SurfaceViewActivity"/>
        <activity android:name=".TextureViewActivity"/>
        <activity android:name=".OpenGLDrawActivity"/>
        <activity android:name=".GLSurfaceViewActivity"/>
        <activity android:name=".FboDrawActivity"/>
        <activity android:name=".JniOpenGLDrawActivity"/>
    </application>

</manifest>